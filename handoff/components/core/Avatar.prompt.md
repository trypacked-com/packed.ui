Round avatar with image or coloured-initials fallback.

```jsx
<Avatar name="Mara Quinn" src="/mara.jpg" size="md" />
<Avatar name="Tom Reyes" /> {/* initials fallback */}
```

Sizes `xs | sm | md | lg | xl`. Set `ring` for a brand status ring.
