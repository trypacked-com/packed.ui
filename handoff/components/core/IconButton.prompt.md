Square or round icon-only button for toolbars and inline row actions. Always pass `aria-label`.

```jsx
<IconButton aria-label="Notifications" variant="soft"><BellIcon/></IconButton>
```

Variants: `solid | soft | outline | ghost`. Sizes `sm | md | lg`. Set `round` for a circle.
