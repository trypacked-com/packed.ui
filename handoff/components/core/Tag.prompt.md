Removable chip for filters and multi-select inputs.

```jsx
<Tag onRemove={() => remove(id)}>Beach</Tag>
```

Pass `onRemove` to show the × button; omit for a static chip. `leadingIcon` adds an icon before the label.
