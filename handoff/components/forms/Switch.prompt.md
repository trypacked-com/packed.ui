On/off toggle with a soft sliding thumb. Use for settings and notification preferences.

```jsx
<Switch label="Flight alerts" checked={on} onChange={setOn} />
```

`onChange` receives `(checked, event)`.
