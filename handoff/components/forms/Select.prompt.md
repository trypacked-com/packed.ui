Native select styled to match Input, with a custom chevron.

```jsx
<Select label="Cabin" options={['Economy','Premium','Business']} />
<Select options={[{value:'lis',label:'Lisbon'}]} />
```

Accepts string or `{value,label}` options. Sizes `sm | md | lg`.
