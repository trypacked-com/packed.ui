Notification toast that mirrors the push alerts clients receive — soft shadow, coloured accent rail, icon chip.

```jsx
<Toast tone="warning" icon={<PlaneIcon/>} title="Gate change" message="TP1234 now departs from B7." time="2m ago" onClose={dismiss} />
```

Tones: `brand | sky | success | warning | danger`.
