Underline tab bar with optional icons and counts. Controlled via `value` + `onChange`.

```jsx
<Tabs value={tab} onChange={setTab} items={[
  {value:'flights', label:'Flights', count:3},
  {value:'stays', label:'Stays'},
]} />
```
